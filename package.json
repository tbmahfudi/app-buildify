{
  "name": "flex-component-library",
  "version": "1.1.0",
  "description": "Modern vanilla JavaScript component library for flexible layouts and UI",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage",
    "test:run": "vitest run",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "build:css": "cd frontend && tailwindcss -i assets/css/app.css -o assets/css/app.build.css --minify",
    "watch:css": "cd frontend && tailwindcss -i assets/css/app.css -o assets/css/app.build.css --watch"
  },
  "keywords": [
    "components",
    "ui",
    "layout",
    "vanilla-js",
    "es6",
    "responsive",
    "accessible"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "@testing-library/dom": "^9.3.3",
    "@vitest/coverage-v8": "^1.0.0",
    "@vitest/ui": "^1.0.0",
    "jsdom": "^23.0.0",
    "tailwindcss": "^3.4.19",
    "vitest": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
